{{- if .Values.developmentTesting.enabled }}
apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.dbservice.name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  name: {{ .Values.dbservice.name }}-nodeport
spec:
  externalTrafficPolicy: Cluster
  internalTrafficPolicy: Cluster
  ports:
    - nodePort: {{ .Values.developmentTesting.nodePorts.dbservice }}
      port: {{ .Values.dbservice.service.port }}
      protocol: TCP
      targetPort: {{ .Values.dbservice.service.port }}
  selector:
    app: {{ .Values.dbservice.name }}
  sessionAffinity: None
  type: NodePort
{{- end }}
