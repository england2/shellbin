apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shellbin.fullname" . }}-test-connection"
  labels:
    {{- include "shellbin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shellbin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
