apiVersion: v1
kind: Service
metadata:
  name: {{ include "shellbin.fullname" . }}
  labels:
    {{- include "shellbin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shellbin.selectorLabels" . | nindent 4 }}
